"""
This module implements fair-networks.

It is composed of several sub-modules:
   fn: main implementation of the model (the model itself and the training procedure)
   rn: "random-network" implementation (one that output a random representation)
   utils: utility classes (mainly, the Options class)
   vfae: "variational fair autoencoders" implementation
"""